package rfinder.structures.components;

import rfinder.structures.nodes.PathNode;

public non-sealed class WalkLink extends PathLink{
    public WalkLink(PathNode destinationNode) {
        super(destinationNode);
    }
}
